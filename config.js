/* =========================================================================
   英语能力测试 · 站点配置
   收件邮箱**不写在这里**——这个页面是公开的，邮箱写进前端会被爬虫抓去发垃圾邮件。
   两个收件地址配置在服务端 apps-script/代码.gs 的 RECIPIENTS 里。
   ========================================================================= */
var EET_CONFIG = {

  /* ------------------------------------------------------------------
     群机器人 webhook：考生开考和交卷时，各推一条消息到考官的群里。
     支持企业微信和飞书，按网址自动识别：
       企业微信  https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxx
       飞书      https://open.feishu.cn/open-apis/bot/v2/hook/xxxxx
     留空则不发通知，考试本身照常可用。

     ⚠️ 这个网址会随页面公开：任何人打开考试页按 F12 都能看到它，
        进而能往你的群里发消息。这是纯静态方案的固有代价，不是配置失误。
        真被人骚扰了，把群机器人删掉重建、换掉这里的网址即可。
     ------------------------------------------------------------------ */
  WEBHOOK_URL: '',

  /* 飞书机器人如果开了「自定义关键词」校验，消息里必须包含该关键词。
     这里的前缀会加在每条消息开头，默认值同时也是个好用的标题。
     （企业微信机器人没有这道校验，留着也无妨） */
  WEBHOOK_KEYWORD: '英语测试',

  /* 备用：POST 到自建服务端的地址（当前方案用不到，留作以后接后端） */
  ENDPOINT_URL: '',

  /* 口语录音是否随通知上传。当前方案为 false：
     群机器人发不了大附件，录音由考生下载结果包后自行发给考官。 */
  SEND_AUDIO: false,

  /* 单个录音超过这个大小就不上传（仅在 SEND_AUDIO 为 true 时有意义） */
  MAX_AUDIO_MB: 12,

  /* 结果页面是否保留本地 zip 下载按钮（邮件已自动发送，这个只是备份手段） */
  KEEP_LOCAL_ZIP: true
};
