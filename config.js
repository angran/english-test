/* =========================================================================
   英语能力测试 · 站点配置
   收件邮箱**不写在这里**——这个页面是公开的，邮箱写进前端会被爬虫抓去发垃圾邮件。
   两个收件地址配置在服务端 apps-script/代码.gs 的 RECIPIENTS 里。
   ========================================================================= */
var EET_CONFIG = {

  /* 邮件中转地址（Google Apps Script 部署后的 /exec 网址）。
     - 如果网页本身就由 Apps Script 托管：留空即可，页面会自动走内部通道（更可靠，有回执）
     - 如果网页托管在 GitHub Pages / 其他静态空间：把 /exec 网址填在这里 */
  ENDPOINT_URL: '',

  /* 是否把口语录音一并上传（存进考官的 Google 云端硬盘，结果邮件里给链接） */
  SEND_AUDIO: true,

  /* 单个录音超过这个大小就不上传，改为提示考生另行发送（避免请求过大失败） */
  MAX_AUDIO_MB: 12,

  /* 结果页面是否保留本地 zip 下载按钮（邮件已自动发送，这个只是备份手段） */
  KEEP_LOCAL_ZIP: true
};
